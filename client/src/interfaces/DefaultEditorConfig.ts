export interface DefaultEditorConfig {
    font: string;
    fontSize: number;
    color: string;
    lineHeight: number;
    cursorWidth: number;
    cursorColor: string;
    selectionColor: string;
    tabSize: number;
}