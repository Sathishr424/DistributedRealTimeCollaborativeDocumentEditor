import { IsEmail, IsNotEmpty, MaxLength, MinLength } from "class-validator";
import {RequestValidation} from "../../interfaces/RequestValidation";

export class DocumentCreationDTO implements RequestValidation {

}